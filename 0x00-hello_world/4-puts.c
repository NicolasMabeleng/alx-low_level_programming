#include <stdio.h>
/**
*main - serves as the entry point for the program
*Return - returns zero by default if no errors else returns non-zero value
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
