#include "main.h"

/**
 * add - adds two integers and returns the results
 * @i: first integer
 * @k: secind integer
 * Return: sum of first integer and second integer
 */
int add(int i, int k)
{
	return (i + k);
}
