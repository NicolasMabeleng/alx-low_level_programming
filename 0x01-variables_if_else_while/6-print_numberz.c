#include <stdio.h>
/**
 * main - serves as the Entry point
 * Return: 0 if no error, non-zero if error
 */
int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
		putchar('\n');
		return (0);
}
